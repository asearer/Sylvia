FROM python:3.11-slim

WORKDIR /app
COPY src/ ./src

# Install dependencies (placeholder, add visualization libraries if needed)
RUN pip install --no-cache-dir --upgrade pip

WORKDIR /app/src

CMD ["python", "main.py"]
